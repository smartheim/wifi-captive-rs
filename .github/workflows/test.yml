name: Build and Test

on: push

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: hecrj/setup-rust-action@master
    - uses: actions/checkout@master
    - name: Install libusb dependency
      run: sudo apt-get install libdbus-1-dev
    - name: Build
      run: apt-get install cargo test
