name: Build and Test

on: push

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: hecrj/setup-rust-action@master
    - uses: actions/checkout@master
    - name: Build
      run: cargo test
